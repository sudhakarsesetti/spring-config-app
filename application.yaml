eureka:
 client:
  fetch-registry: true
  register-with-eureka: true
  service-url: 
   defaultZone: ${EUREKA_SERVER_ADDRES:http://localhost:8761/eureka}
 instance:
   prefer-ip-address: true          
